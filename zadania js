1.
const div = document.createElement("div")
div.textContent = "div"

const body = document.querySelector("body")
body.appendChild(div)

2.
const czerwony = document.querySelector("h1")
czerwony.style.color = "red"

3.
const obrazek = document.querySelector("img")
obrazek.src = "https://loremflickr.com/566/320/dog"

4.
const paragraf = document.querySelector("main")
const paragrafRemove = paragraf.querySelector("p")
paragraf.removeChild(paragrafRemove)

5.
const div2 = document.createElement("div")
div2.textContent = "div"
div2.classList.add('nowak');

const body2 = document.querySelector("body")
body2.appendChild(div2)


6.
const li4 = document.createElement("li")
li4.textContent = "li"
li4.classList.add('active');

const activeListItem = document.querySelector('.active');
activeListItem.classList.remove('.active');


const body = document.querySelector("body")
body.appendChild(li)

7.
const button = document.querySelector("button")
button.textContent = "Przycisk!"

8.
const input = document.querySelector('input');
input.value = '___________________'

9.
const newListElement = document.createElement('li');
const unorderedList = document.querySelector('ul');
unorderedList.appendChild(newListElement);
newListElement.textContent = "div"

10.
const czerwony = document.querySelector("body")
czerwony.style.backgroundColor = "blue"

11.
const newDiv = document.querySelector('div');
newDiv.classList.add('new-div');
const header = document.querySelector('header');
header.appendChild(newDiv);

12.
const h1 = document.createElement("h1")
h1.textContent = "h1"

const body = document.querySelector("body")
body.appendChild(h1)

const headers = document.querySelectorAll('h1');
if (headers.length > 1) {
  headers[1].style.color = 'green';
}

13.
const obrazek = document.querySelector("img")
obrazek.dataset.src = "https://loremflickr.com/566/320/dog"
obrazek.removeAttribute('src');

14.
const paragraphs = document.querySelectorAll('p');
for (let i = 1; i < paragraphs.length; i += 2) {
  paragraphs[i].style.display = 'none';
}

15.
const listItems = document.querySelectorAll('li');
if (listItems.length >= 3) {
  const thirdListItem = listItems[2];
  thirdListItem.classList.add('selected');
}

16.
function addElement() {
  var list = document.querySelector('details ul');
  var newItem = document.createElement('li');
  newItem.textContent = 'Nowy element';
  list.insertBefore(newItem, list.firstChild);
}

17.
function removeElements() {
  var listItems = document.querySelectorAll('details > ul > li');
  for (var i = 1; i < listItems.length; i += 2) {
    listItems[i].remove()
  }
}

18.
function addParagraph(tekst) {
  event.preventDefault();
  var text = document.querySelector('#newParagraph').value;
  var newParagraph = document.querySelector('details > section > p');
  newParagraph.textContent = text;
  document.querySelector('section').appendChild(newParagraph);
  document.querySelector('#newParagraph').value = ' '
  }




ZADANIA V2
1.
const title = document.querySelector('.title')
title.textContent = 'DOM Training Arena'

2.
const item = document.querySelector('.nav-item a')
item.firstChild.textContent = 'Start'

3.
const lista = document.querySelector(".menu-list li")
const text = lista.textContent;
console.log(text);

4.
const div = document.querySelector(".card.highlight")
div.classList.add('active');

5.
const karta = document.querySelector(".card .card-header")
karta.textContent = 'Zmieniona karta'

6.
const dodatek = document.querySelector("details .info-text")
dodatek.textContent = 'Jan Paweł 2 papież polak'

7.
const imie = document.querySelector("label input")
imie.setAttribute("value", 'Jan')
const cos = imie.value
console.log(cos)

8.
const wybierz = document.querySelector(".select")
wybierz.value = "de"

9.
const content = document.querySelector(".checkbox")
content.checked = true

10.
const deep = document.createElement("class")
deep.classList.add('highlight');

const text = document.querySelector(".deep-text")
text.appendChild(deep)

11.
const list2 = document.createElement("li")
list2.textContent = "Nowy Element"
const dynam = document.querySelector("#dynamic-list")
dynam.appendChild(list2)

12.
const li = document.createElement("li")
li.textContent = "Nowy Element"
const li2 = document.createElement("li")
li2.textContent = "Nowy Element"
const li3 = document.createElement("li")
li3.textContent = "Nowy Element"
const menu = document.querySelector(".menu-list")
menu.appendChild(li)
menu.appendChild(li2)
menu.appendChild(li3)

13.
const create = document.createElement("div'")
create.classList.add("card)
const sekcja = document.querySelector(".cards-section")
sekcja.appendChild(create)

14.
const box = document.createElement(".box")
box.textContent = "Nowy Element"
const stop = document.querySelector(".boxes-container")
stop.appendChild(box)

15.
const p = document.createElement("p")
p.textContent = "Jan Paweł II Papież Polak"
const area = document.querySelector(".dynamic-area")
area.appendChild(p)

16.
function addElement() {
  var lista = document.querySelector('#insertion-container');
  var nowyItem = document.createElement('.box');
  nowyItem.textContent = 'Nowy element';
  lista.insertBefore(nowyItem, lista.firstChild);
}

17.
const list = document.createElement("li")
list.textContent = "Nowy Element"
const dynamic = document.querySelector("#dynamic-list")
dynamic.prepend(list)

18.
const s = document.createElement("span")
const karta = document.querySelectorAll(".card-header")
karta.appendChild(s)

19.
const pa = document.createElement("p")
pa.textContent = "Jan Paweł II Papież Polak"
const details = document.querySelector("details")
details.appendChild(pa)

20.
const przycisk = document.createElement("button")
przycisk.textContent = "przycisk"
const button = document.querySelector(".playground")
button.append(przycisk)

21.
const remove = document.querySelectorAll(".removable")
for (let element of remove ){
  element.remove()
}

22.
const dynamo = document.querySelector("#dynamic-list > li:nth-last-child(1)")
const lisc = document.querySelector("#dynamic-list")
lisc.removeChild(dynamo)

23.
const card = document.querySelector(".card[data-id='1']")
card.remove()

24.
const boxy = document.querySelectorAll(".box")
const wp = document.querySelector("#insertion-container")
boxy.forEach((elements)=>
wp.remove(boxy))

25.
const detale = document.querySelector("details")
detale.remove()

26.
const pw = document.querySelectorAll("input")
pw.forEach(inputs => {
  console.log(inputs.name)
})

27.
const mail = document.querySelector("input[name='email']")
mail.name = "user-email"

28.
const light = document.querySelector(".card-highlight")
light.setAttribute('data-test', 123)

29.
const menu = document.querySelectorAll(".menu-list li")
menu.forEach((elementy)=>
elementy.textContent = "Zmieniono")

30.
const kkarta = document.querySelector(".card")
kkarta.classList.add('processed');
const data2 = document.querySelector(".card")
data2(data.status) = "done"

31.
const element = document.querySelector("#add_item_bttn")
element.addEventListener("click", () => {
  const item_input = document.querySelector("#item-input").value

  const li = document.querySelector("li")
  li.textContent = item_input
  document.querySelector("#dynamic-list").appendChild(li)
  document.querySelector("#item-input").value
 = ' '
})

32.
const item_input = document.querySelector("#item-input")
item_input.addEventListener("input", (event) => {
  console.log(event.target.value)
})

33.
const insert_before_btn = document.querySelector("#insert-before-btn")
insert_before_btn.addEventListener("click", () => {
  const box = document.querySelector("div")
  box.classList.add("box")
  const insertion_conteiner =  document.querySelector("#insertion-container")
  insertion_conteiner.prepend(box)
})

34.
const add_box_btn = document.querySelector("#add-box-btn")
add_box_btn.addEventListener("click", () => {
  const box = document.querySelector(".boxes-container")
  box.classList.add("box")
  const insertion_conteiner =  document.querySelector(".boxes-container")
  insertion_conteiner.prepend(box)
})

35.
const removable = document.querySelectorAll(".removable")

for(let element of removable){
  element.addEventListener("click", (event)=> {
    event.target.remove();
  })
}

36.
const test_form = document.querySelector("#test-form")
test_form.addEventListener("sumbit", (event) => {
    event.preventDefault()
})

37.
const select = document.querySelector(".select")
select.addEventListener("change", (event) => {
  console.log(event.target.value)
})

38.

39.
const cards = document.querySelectorAll(".card")
for(let card of cards){
  card.addEventListener("mouseover", (event) => {
    event.target.style.backgroundColor = "green"
  })

  card.addEventListener("mouseout", (event)=>{
    event.target.style.backgroundColor = "red"
  })
}

40.
const input = document.querySelector("input[name = 'name]")

input.addEventListener("keydown", (event) =>{
  if(event.key === "Enter") {
    console.log(event.target.value)
  }
})

ZADANIA V3
1.
const header = document.querySelector("#main-header")

2.
const li = document.querySelectorAll("nav > li")

3.
const delete_trigger = document.querySelector(".delete-trigger")
const firstChild = delete_trigger.firstElementChild;

4.
const content_area = document.querySelectorAll("#content-area > section")

5.
const data_secret = document.querySelector("[data-secret]")

6.
const data_link = document.querySelector("a[data-link='home']")
const link = data_link.href

7.
const trigger_btn = document.querySelector("#trigger-btn")
trigger_btn.closest(".level-1")

8.
const tr = document.querySelectorAll("tbody > tr")

9.
const figcaption = document.querySelector("figcaption")
const main_image = figcaption.previousSibling("#main-image")

10.
const checkbox = document.querySelectorAll("legend > checkbox")

11.
const username = document.querySelector("#username")
const rodzic = username.parentElement("#username")

12.
const notification_wrapper = document.querySelector (".notification-wrapper")
const dziecko = notification_wrapper.childNodes(".notification_wrapper")

13.
const h1 = document.querySelector("h1").textContent

14.
const notification_list = document.querySelector("#notification-list")
const ostatni = notification_list.lastElementChild("#notification-list")

15.
const nest = document.querySelectorAll(".nest.level-2")

16.
const li = document.createElement("li")
const nav = document.querySelector(".nav-list")
nav.prependChild(li)

17.
const button = document.createElement("button")
button.textContent = "Logowanie"
const nav = document.querySelector("nav")
nav.appendChild(button)

18.
function myCreateFunction() {
  var table = document.querySelector("#user-table");
  var row = table.insertRow(0);
  var cell1 = row.insertCell(0);
  var cell2 = row.insertCell(1);
  cell1.innerHTML = "103";
  cell2.innerHTML = "Piotr";
}

19.
const newP = document.createElement("p")
newP.textContent = 'Uwaga To Papież'
const forms_demo = document.querySelector("#forms-demo")
forms_demo.prepend(newP)

20.
const ul = document.createElement("ul")
const ul2 = document.createElement("ul")
const ul3 = document.createElement("ul")
ul.textContent = "asdasddadsads"
ul2.textContent = "adasdasdadsddddddddd"
ul3.textContent = "asdadasdadsddddd"
const skill_list = document.querySelector("#skill-list")
skill_list.appendChild(ul)
skill_list.appendChild(ul2)
skill_list.appendChild(ul3)

21.
const p = document.querySelector(".notification-item, .alert");
console.log(p)
const p2 = p.cloneNode(true);
const notification_wrapper = document.querySelector(".notification-wrapper")
notification_wrapper.appendChild(p2)

22.
const span = document.createElement("span")
span.textContent = "(Wymagane)"
const label = document.querySelector(".form-group > label")
label.appendChild(span)

23.
const hr = document.createElement("hr")
const content = document.querySelector("#content-area")
for(const child of content.children){
  child.append(hr)
}

24.
const innerHTML = document.querySelector("footer")
innerHTML.innerHTML = "Polityka prywatnośći <a href='#'>CLICK HERE</a>"

25.
const h1 = document.querySelector("h1")
h1.prepend("🚀")

26.
const option = document.createElement("option")
option.textContent = "Admnistrator"
const role_select = document.querySelector("#role-select")
role_select.appendChild(option)

27.
const style = document.createElement("style")
const head = document.querySelector("head")
head.appendChild(style)
const body = document.querySelector("body")
body.style.backgroundColor = "lightgrey"

28.
const button = document.createElement("button")
button.textContent = "Anuluj"
const form = document.querySelector("#complex-form")
form.appendChild(button)

29.
const div = document.createElement("div")
div.classList.add("alert-box")
const tabela = document.querySelector("#user-table")
tabela.prepend(div)

30.
const mark = document.createElement("mark")
const szmaragd = document.querySelector(".level-3 , .nest")
szmaragd.appendChild(mark)

31.
const kolor = document.querySelector("h1")
kolor.style.color = "darkblue"

32.
const admin_only = document.querySelector(".admin-only")
admin_only.setAttribute("style", "display: block")

33.
const img = document.querySelector("#main-image")
img.setAttribute("src", "https://loremflickr.com/566/320/dog")

34.
const highlight = document.querySelectorAll(".notification-item")
highlight.classList.add("highlight")

35.
const placeholder = document.querySelector("#username")
placeholder.setAttribute("placeholder", "Podaj imie...")

36.
const data_priority = document.querySelector("[data-priority]")
console.log(data_priority.dataset.priority)

37.
const datetime = document.querySelector("time")
datetime.setAttribute("datetime", "29-04-2026")

38.
const username = document.querySelector("#username")
username.setAttribute("placeholder", "disabled")

39.
const figcaption = document.querySelector("figcaption")
figcaption.textContent = "Nowy opis obrazka"

40.
const border = document.querySelector("table")
border.style.border = "2px solid black"

41.
const nest = document.querySelector("level-3, .nest")
nest.classList.remove("nest")

42.
const sukces = document.querySelector(".success")
sukces.style.backgroundColor = "lightgreen"

43.
const atrybut = document.querySelector("a")
for(const child of atrybut.children){
  child.setAttribute("target", "_blank")
}

44.
const moderator = document.querySelector("#username")
moderator.setAttribute("value", "Moderator")

45.
const active = document.querySelector(".active")
const trigger_btn = document.querySelector("#trigger-btn")
if (trigger_btn.contains(active)) {
  console.log('Klasa jest')
}

46.
const notification_list = document.querySelector("#notification-list")
const notification = document.querySelector(".notification-item")
notification_list.removeChild(notification)

47.
const semantic_demo = document.querySelector("#semantic-demo")
const semantic = semantic_demo.childNodes
semantic_demo.remove(semantic)

48.
const jedendwa = document.querySelector("tr[data-user-id='102']")
jedendwa.remove()

49.
const description = document.querySelectorAll(".description")
description.forEach((des) => {
  des.remove()
})

50.
const footer = document.querySelector("footer")
footer.remove()
